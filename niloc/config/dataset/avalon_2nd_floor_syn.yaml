# @package _group_
# Dataset config for fabricated Avalon synthetic trajectories (issue #10).
#
# root_dir, train_list, and val_list must be set to the fabricated dataset
# output directory. Pass them as CLI overrides to train_synthetic.sh:
#
#   ./train_synthetic.sh avalon_2nd_floor /path/to/niloc/outputs/fabricated/avalon_2nd_floor
#
# The fabricated dataset is generated by running (from the niloc repo root):
#   uv run python -m preprocess.synthetic_data.fabricate
# Output goes to: outputs/fabricated/avalon_2nd_floor/
name: avalon_synthetic
root_dir: ???
train_list: ???
val_list: ???
